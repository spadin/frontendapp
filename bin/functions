#! /usr/bin/env sh

ENV=${ENV:-"development"}

setup() {
  cd "`dirname "$0"`/.."
}

compose() {
  env=${ENV:-"development"}
  docker-compose -f config/$env/docker-compose.yml $@
}

watch_command() {
  if [ "$1" == "--watch" ]; then
    echo "${@:2}"
  fi
}
