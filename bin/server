#! /usr/bin/env sh

cd "`dirname "$0"`/.."

NODE_ENV=${ENV:-development}

docker-compose run --rm -e NODE_ENV=$NODE_ENV --service-ports frontend \
  npm run webpack-dev-server -- \
    --hot \
    --inline \
    --host=0.0.0.0 \
    --content-base=bundle
