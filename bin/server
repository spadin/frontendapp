#! /usr/bin/env sh

cd "`dirname "$0"`/.."

docker-compose run --rm --service-ports frontend \
  npm run webpack-dev-server -- \
    --hot \
    --inline \
    --host=0.0.0.0 \
    --content-base=bundle
